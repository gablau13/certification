{% extends 'base.html.twig' %}

{% block title %}gestion des utilisateurs
{% endblock %}

{% block body %}
	<style>
		.example-wrapper {
			margin: 1em auto;
			max-width: 800px;
			width: 95%;
			font: 18px / 1.5 sans-serif;
		}
		.example-wrapper code {
			background: #F5F5F5;
			padding: 2px 6px;
		}
	</style>
	{% if app.user %}
		<div class="mb-3">
			You are logged in as
			{{ app.user.username }},
			<a href="{{ path('app_logout') }}">Logout</a>
		</div>
	{% endif %}
	<div class="container-fluid">
		<h1>Profil utilisateurs</h1>
		<table class="table">
        
            
       
			<tr>
				<th>Prénom</th>
				<th>Nom</th>
				<th>Email</th>
				<th>Action</th>

			</tr>
{% for user in user %}
			<tr>
				<td>{{user.prenom}}</td>
				<td>{{user.nom }}</td>
				<td>{{user.email}}</td>
				<td>
					<a href="{{path('profil_edit', {'id': user.id})}}">
					<i class="bi bi-pencil"></i>
					</a>
				
					<a href="{{path('profil_user_annonce', {'id': user.id})}}">
					<i class="bi bi-eye"></i>
					</a>
				</td>
			</tr>
 {% endfor %}

		</table>
       

	</div>


{% endblock %}
